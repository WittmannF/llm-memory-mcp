# Contexts

Real source-of-truth contexts live here locally, but are ignored by git by default.

Example local paths:

```text
source-of-truth/contexts/saude-pai/
source-of-truth/contexts/trabalho/
source-of-truth/contexts/imoveis/
```

Do not commit real private context data to this public repository.
